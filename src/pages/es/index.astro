---
import Layout from '../../layouts/Layout.astro';
import AIPricingHub from '../../components/AIPricingHub';
import { aiServices } from '../../data/ai-data';
import { getLocaleAndTranslations, type SupportedLocale } from '../../utils/i18n';

const locale: SupportedLocale = 'es';
---

<Layout 
  title="AI Pricing Hub - Comparativa de herramientas y servicios de IA" 
  description="Encuentra la solución de IA perfecta para tus necesidades con nuestra herramienta de comparación de precios."
>
  <AIPricingHub services={aiServices} locale={locale} client:load />
</Layout>

